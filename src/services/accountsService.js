// Service router — components import from here, never from mocks/ directly.
// Swap to the real implementation here when ready; no component changes needed.
export { getAccounts } from './mocks/accountsService.js'
