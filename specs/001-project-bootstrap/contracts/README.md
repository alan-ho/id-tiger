# Contracts

No API contracts exist for the bootstrap release. This directory will contain
service interface contracts (function signatures, request/response shapes) when
backend-connected features are planned.
